import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  input: {
    width: '100%',
    height: 40,
    borderWidth: 1,
    paddingHorizontal: 20,
    paddingVertical: 30,
  },
});
